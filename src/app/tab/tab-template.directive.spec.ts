import { TabTemplateDirective } from './tab-template.directive';

describe('TabTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new TabTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
